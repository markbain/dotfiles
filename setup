
#!/bin/bash

#--------------------------------------------------------------------#
#
# Set up colors

RED='\033[0;31m'
GREEN='\033[0;32m'
LIGHTBLUE='\033[0;34m'
NC='\033[0m' # No Color

#--------------------------------------------------------------------#
#
# Install NVM, NodeJS, NPM

# NVM v0.33.8
wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm

# Now use NVM ton install Node
nvm install node

# Grunt
# We are installing this locally, as it runs much faster than in a VM.
npm install -g grunt-cli

#--------------------------------------------------------------------#
#
# Add repos

sudo add-apt-repository ppa:obsproject/obs-studio ppa:webupd8team/sublime-text-3 ppa:gnome-terminator ppa:jeffreyratcliffe/ppa -y

#--------------------------------------------------------------------#
#
# Add Apt key for Spotify

sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys BBEBDCB318AD50EC6865090613B00F1FD2C19886
echo deb http://repository.spotify.com stable non-free | sudo tee /etc/apt/sources.list.d/spotify.list

#--------------------------------------------------------------------#
#
# Update

sudo apt-get update

#--------------------------------------------------------------------#
#
# Automatic install

sudo apt install -y chromium-browser compiz compizconfig-settings-manager compiz-plugins curl digikam git git-flow grip filezilla ghostscriptmeld gimp gpick gscan2pdf htop inkscape imagemagick nautilus-dropbox obs-studio shutter spotify-client sublime-text-installer terminator unrar vagrant vim vokoscreen zsh

#--------------------------------------------------------------------#
#
# Make Zsh default shell

chsh -s $(which zsh)

#--------------------------------------------------------------------#
#
# Install Oh My ZSH

0>/dev/null sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

#--------------------------------------------------------------------#
#
# Install patched Powerline fonts for use with agnostic Zsh theme

git clone https://github.com/powerline/fonts.git
cd fonts
./install.sh
cd ..
rm -rf fonts

#--------------------------------------------------------------------#
#
# WP-CLI 

curl -O https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
php wp-cli.phar --info
chmod +x wp-cli.phar
sudo mv wp-cli.phar /usr/local/bin/wp
wp --info

#--------------------------------------------------------------------#
#
# Announce end of automatic install:

echo -e "${GREEN}Automatic installations completed.\n${NC}"

#--------------------------------------------------------------------#
#
# Virtualbox

sudo apt install -y virtualbox virtualbox-ext-pack

#--------------------------------------------------------------------#
#
# 	Set default applications
#
#--------------------------------------------------------------------#

sudo update-alternatives --all

echo -e "The following packages must be installed manually:"
echo -e "${RED}* Crashplan\n${NC}"
echo -e "${RED}* Firefox\n${NC}"
echo -e "${RED}* Upwork client\n${NC}"
echo -e "${RED}* Slack client\n${NC}"
echo -e "${RED}* Skype client\n${NC}"
echo -e "${RED}* Gitkraken\n${NC}"
